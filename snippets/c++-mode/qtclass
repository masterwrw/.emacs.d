# -*- mode: snippet -*-
# name: qtclass
# key: qtcls
# --
class ${1:Name} : public QObject
{
    Q_OBJECT
public:
    ${1:$(yas/substr yas-text "[^: ]*")}(QObject *parent=Q_NULLPTR);
    ${2:virtual ~${1:$(yas/substr yas-text "[^: ]*")}();}

signals:

public slots:

private:

};
$0